ENCRYPTION_KEY=  # base64-encoded 32-byte AES key: python3 -c "import os,base64; print(base64.b64encode(os.urandom(32)).decode())"
SUPABASE_URL=    # https://<project-ref>.supabase.co
SUPABASE_SERVICE_KEY=  # service_role key from Project Settings → API
